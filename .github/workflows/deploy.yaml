name: Docker Login Test

on: [workflow_dispatch]  # allows manual run from Actions tab

jobs:
  test-docker-login:
    runs-on: ubuntu-latest
    steps:
      - name: Docker login
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
